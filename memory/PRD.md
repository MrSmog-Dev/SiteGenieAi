# SiteGenie — PRD

## Original Problem Statement
An online store that creates website templates for businesses without a website. Subscription-based (Monthly, 3-Month, Annual). Template generator works via credits; subscribers can purchase extra credits after hitting plan limits. Later additions: native Stripe subscriptions, usage-based credit model, Owner bypass role, multi-step Agentic AI pipeline, publishing/hosting with vanity URLs, SEO meta tags, branded OG images, ZIP exports.

## Users
- Small business owners without a website (primary)
- Owner/admin account with unlimited credits (bypass role)

## Architecture
- Frontend: React + Tailwind + Shadcn (dark, brand blue #0055FF, sharp-edged design system)
- Backend: FastAPI modular (`routes/`, `services/`, `models.py`, `config.py`, `security.py`), MongoDB (Motor)
- Auth: JWT email/password + Emergent Google Auth
- Payments: Stripe LIVE mode (native subscriptions + credit packs, webhooks)
- AI: Emergent LLM Key — Claude Sonnet 4.6 (Quality, 2-pass) / Haiku 4.5 (Economy)
- Publishing: vanity slugs `/api/p/{slug}`, server-rendered SEO HTML, Pillow OG cards `/api/og/{slug}.png`, ZIP export

## Implemented (history)
- 2026-05/06: Full MVP — auth, plans, generator, credits, Stripe subscriptions & packs, Owner role
- 2026-06: Agentic 2-pass pipeline + Economy/Quality toggle; backend modularization; publish flow with vanity slugs; SEO meta payload; branded OG card generator; ZIP export; live iframe thumbnails; pytest regression suites (test_phase1, test_phase3, test_publish_flow)
- 2026-06 (prod): User deployed to production at https://sitegenie.dev
- 2026-07-03: **Emergent-style chat-first landing page redesign** — centered hero with glowing prompt box, typewriter cycling placeholder, 6 suggestion chips (coffee/barber/fitness/restaurant/law/florist), Enter-to-start, personalized greeting for logged-in users, pricing/features kept below fold. Fixed stale "3 free credits" → 15 on Register page.
- 2026-07-03: **Chat-style follow-up flow on landing** (approved enhancement) — after typing an idea or picking a chip, the agent asks 2 scripted follow-ups (business name → brand vibe with 6 quick-select vibe chips or free text) in a chat thread with typing indicator, Skip and Start-over controls. Full brief stored as JSON in `sessionStorage.sg_pending_brief` {description, industry, business_name, brand_keywords, style} and pre-fills 5 Generator fields. Brief carried through email login/register AND Google OAuth callback into Generator; Login/Register show "brief saved" banners and route to `/generate` when a brief exists. Tested by testing agent: 8/8 scenarios pass (iteration_8.json).

## Known Issues / Blockers
- ~~P0 BLOCKED: Emergent LLM Key budget exceeded~~ **RESOLVED 2026-07-03** — user topped up; verified end-to-end with owner login + `POST /api/templates/generate` returning 65KB HTML.
- Stripe is LIVE mode — be careful with test purchases
- Sonnet 4.6 (Quality mode) is flaky right now — sometimes returns in ~90s, sometimes stalls >4min. Mitigated 2026-07-03 with a 240s per-call timeout in `services/llm.py::_call_llm` that fails the job cleanly instead of hanging; Economy mode (single-pass Haiku) reliably completes in ~30-60s.

- 2026-07-03: **P2 Analytics counter** — non-bot views counted on `/api/p/{slug}` and domain-served pages (`views_total` + `views_daily` per date, bot UAs filtered by regex); `GET /api/templates/{id}/stats` returns totals + 14-day daily series; UI: views badge on My Websites cards, views chip in TemplateView header, total + 14-bar sparkline in share dialog. Tested: iteration_9.json 100% pass.
- 2026-07-03: **P3 Custom domain mapping** — `PUT/DELETE /api/templates/{id}/domain` (normalize + validate + uniqueness w/ unique sparse index), `POST .../domain/verify` (DNS A-record IP comparison vs app host), `GET /api/public/domain/{host}` public payload, host-based serving in App.js → DomainSite.jsx (foreign hostname renders published site full-screen). Share dialog UI: connect input, Pending DNS/Verified badges, CNAME instructions, verify + remove. NOTE: for production, arbitrary domains must also be routed by the hosting ingress (user may need Emergent support to attach domains to the deployment). Tested: iteration_9.json 100% pass.
- 2026-07-03: **LLM call timeout + graceful error surfacing** — added 240s `asyncio.wait_for` around every `_call_llm` in `services/llm.py` and taught `_fail_job` to recognize `asyncio.TimeoutError` and write a user-friendly "AI took too long, try again — Economy mode is faster" message to the job. Prevents jobs from hanging forever on Sonnet 4.6 stalls (E2E validated by main agent).
- 2026-06 (rendered 07-03): **Template Market** — public `/market` page selling pre-made templates at $200–$500, priced automatically by an **AI Pricing Agent** (`services/pricing_agent.py`: code-depth metrics + Haiku analysis → price/tier/summary/highlights; heuristic fallback if LLM unavailable). Owner-only listing (`POST /api/market/list`, sell/delist buttons in TemplateView + market page), one-time Stripe Checkout per template (`POST /api/market/{id}/checkout`, kind=market_purchase in payment_transactions, fulfilled via `apply_payment` → `services/market.py::fulfill_market_purchase`), buyer gets an editable copy with `purchased=true` → **FREE unlimited AI edits (credit check bypassed)** + ZIP export; "Most Popular" badge from views/purchases score; listing previews served at `GET /api/market/{id}/preview` (view-counted with `?count=1`, bot-filtered). Frontend: Market.jsx grid with live iframe thumbs, MarketSuccess.jsx post-checkout page, Owned badges in MyTemplates/TemplateView, Market nav in dashboard sidebar + landing. Tested: iteration_11.json 100% (17/17 backend, 7/7 frontend); pytest suite at backend/tests/test_market.py.

- 2026-07-03 (later): **AI Team — 12 autonomous agents (Sintra-style, owner-only)** — `/team` page with roster of 12 personality-driven agents grounded in LIVE business data (`services/agents.py::business_snapshot()` injects real users/revenue/market/views/jobs stats into every reply): Titan (COO/briefings), Nova (strategy), Atlas (data), Ledger (finance), Quill (copy), Ivy (SEO), Blaze (social), Mara (email), Rex (sales), Halo (support), Forge (template builder/curator), Zephyr (growth). Multi-turn chats persisted in `agent_chats` (per user+agent), quick-action chips per agent, clear-chat, generated 3D avatars in `frontend/public/agents/`. **Forge real action**: `POST /api/agents/forge/build {brief}` → invents brand → builds site (Sonnet) → AI-prices → lists on Market autonomously (background `agent_jobs`, polled by UI; verified live: "Pawsitive Studio" $280). Haiku for chats, Sonnet for builds. All `/api/agents/*` owner-gated. Tested: iteration_12.json (frontend 100%; the one backend fail—Forge timeout—fixed & re-verified live).
- 2026-07-03 (later): **Quality→Economy auto-fallback shipped** (was P3): `services/llm.py::_call_build_llm` retries on Haiku when the Sonnet build times out at 240s — applied to user generations, edits, Forge builds and market seeding.
- 2026-07-03 (later): **Market fully seeded** — 9 active listings ($280–$320, all AI-priced): Ember & Oak, IronPulse Fitness, Northwind Creative, Lumen Studio, Flowdesk, Velvet & Sage, Harborline Realty, Driftwood Coffee Co. + Forge-built Pawsitive Studio.

- 2026-07-03 (later): **Anime avatars** — regenerated all 12 AI Team avatars as ORIGINAL characters in a One Piece-inspired shonen pirate-adventure art style (user is a One Piece fan; no actual OP characters used). Files overwritten in `frontend/public/agents/{id}.png`.

- 2026-07-03 (later): **Rex Lead Hunter (active sales agent)** — user-approved criteria: no-website leads = 15+ reviews AND rating ≥3.5 (no cap); weak-website leads = scan score ≤65 (≤40 = HOT tier, 41-65 warm); traction proxy = review volume + manual URL scan. Built: `services/leads.py` (0-100 website scoring: 70pts deterministic checks [HTTPS, viewport, responsive, SEO meta, OG, favicon, contact info, copyright freshness, speed, modern markup] + 30pts Haiku design review with issues + sales pitch; SSRF guard; Google Places hunt via `GOOGLE_PLACES_API_KEY` env — NOT YET CONNECTED, endpoint returns 424 + UI shows connect-key notice), `routes/leads.py` (owner-only CRUD: GET /api/leads, POST /api/leads/scan, POST /api/leads/hunt, GET /api/leads/hunt/status, PATCH/DELETE /api/leads/{id}; statuses new/contacted/won/lost, dedupe by domain/place_id). Rex's chat now includes live `sales_leads` summary in the business snapshot; personality/role updated to "Sales Closer & Lead Hunter" + "Who do I pitch first?" quick action. Frontend: `components/RexLeadPanel.jsx` (scan form + result breakdown, hunt form/locked notice, lead board with status dropdowns) toggled from Rex's chat header. Self-tested end-to-end: example.com→42/100 warm lead added; stripe.com→78 not a lead; SSRF 400; PATCH/DELETE ok; hunt 424; Rex chat cited live lead board; UI panel verified via screenshot.

- 2026-07-03 (later): **Hands-off automation loop** (`services/automation.py`, started at server startup; 15-min tick; Mongo `automation_state` claims; stale-job cleanup after restarts): (1) **Titan daily briefing** — auto-posted to Titan's chat after 11:00 UTC daily (verified live). (2) **Ivy daily SEO article** — after 9:00 UTC daily, writes one 1200-1800-word SEO article to `blog_posts`, served server-rendered at `/api/blog` + `/api/blog/{slug}` + `/api/blog/sitemap.xml` (`services/blog.py`, `routes/blog.py`: canonical/OG/JSON-LD, FAQ, 4-6 keyword-rich internal links to / /market /pricing + interlinks; babylovegrowth-style). Manual trigger: POST /api/agents/ivy/blog + "Write article now"/"View blog" buttons in Ivy's chat; Blog link in landing nav. First article live: "Local Service Pages: Drive More Qualified Leads" (1,418 words, 10 internal links). (3) **Forge weekly template drop** — picks an uncovered niche and builds+prices+lists a Market template weekly (verified live: "GreenNest" — Eco-Friendly Product Marketplace, $280; Market now 10 listings; survived a Sonnet timeout via Haiku fallback). (4) **Rex→Forge demo handoff** — marking a lead "Contacted" auto-builds AND publishes a demo site for that business (real name/phone kept), lead gets demo link (Demo button on Lead Board → /api/p/{slug}), Rex posts the pitch message (verified live E2E, test artifacts cleaned). Agents post automated updates into their own chats via `post_agent_message`.

- 2026-07-04: **Blog images + separation + Blaze distribution** — Ivy's articles now embed 3 images each ([IMAGE:] markers → `services/blog_images.py`: Openverse free CC stock search first, Gemini Nano Banana AI generation fallback via Emergent key; stored in Mongo `blog_images`, served at GET /api/blog/img/{id} with immutable cache). First article backfilled. Blaze auto-posts a ready-to-use X+LinkedIn distribution kit to his chat on every publish. Blog link REMOVED from landing nav (landing = products/templates only per user), added to Market page nav instead.
- 2026-07-04: **Three flagship templates (user goal: $500 grade)** — `build_flagships.py` (core Sonnet build → 3 small addendum-injection upgrade calls: behaviors/gallery+slider/pricing+FAQ+form — full-HTML rewrites always timed out, addendum strategy is the reliable pattern) + `deepen_flagships.py` depth pass + polish pass. Pricing agent recalibrated with explicit price bands ($200-275/$280-360/$370-460/$470-500 flagship criteria) because the LLM grader systematically avoided its own maximum. FINAL: Auréa Med Spa $500, Sterling & Vale $485, Hartwell & Cross LLP $480 (all Premium, 200-245KB, 14-16 sections, working lightbox gallery, sliders, multi-step forms, FAQ accordions, scroll-spy, counters, theme toggle, 13-19 keyframes, 43-54 JS listeners). Market now 13 listings, flagships lead the grid.
- 2026-07-04: **Chat-style Generator + AI Model & Quality Tier selectors + Flagship hero + Owner price override** — three tasks approved by user (1.a/2.a/3.a) and shipped in one turn:
  1. **Generator.jsx** redesigned as a chat-first UI (roles: assistant + user bubbles, stage machine: idea→name→industry→vibe→ready, restart button, brief accordion syncing with chat, live preview iframe still on right). New selectors: `data-testid='model-selector'` (Sonnet 4.6 / Haiku 4.5) and `data-testid='tier-selector'` (Economy / Quality / **Premium**).
  2. **Quality=Premium** in `services/llm.py`: after the standard 2-pass Quality build, runs an extra `_premium_polish` LLM pass (BUILD_MODEL) that injects scroll-triggered animations, gallery/slider, counters, FAQ accordion, richer multi-step contact form, scroll-progress bar. Guards short/invalid output and gracefully falls back to the quality build on timeout.
  3. **GenerateInput.model** field lets users pick the build model per-generation (persisted on template so regenerate reuses it).
  4. **Market.jsx** now renders a distinct **FLAGSHIP COLLECTION** hero band (amber gradient, `data-testid='flagship-hero'`) at the top for any listing with `tier === "Premium"` OR `price_usd >= 400`. Rest of listings continue below under "More templates".
  5. **PUT /api/market/{market_id}/price** (owner-only, `MarketPriceInput{price_usd}`): bypasses the AI Pricing Agent, sets `priced_by="owner_override"`, records `price_updated_at`. Pencil-icon button [`data-testid='override-{id}'`] on every market card (owner only) opens `window.prompt` to change price with toast on success. Validates $1-$100,000, 400 on invalid, 403 non-owner, 404 unknown listing.
  Tested by testing agent (iteration_13.json): 100% frontend + full backend coverage, no action items, all E2E flows pass (chat progression, vibe chips, tier toggle, preview iframe render, override toast).

- 2026-07-04 (later): **True inter-agent communication (user-reported bug: Forge/Halo "sent" things to teammates but nothing was ever delivered — pure roleplay)**. Built 3 systems in `services/team.py`:
  1. **Shared Team Memory (memo bus)** — after every agent chat reply, a Haiku detection call (`detect_and_route_memos`) extracts real commitments ("I'll send this to Quill") into `team_memos` docs; each memo is delivered into recipient agents' chats as "📨 Memo from X" AND injected into ALL agents' system prompts via `team_memory_context()` (in `services/agents.py`). Agents are instructed: memos are real, acknowledge them, never claim past sends not on the board. VERIFIED: Halo's finalized FAQ auto-delivered to Quill; Quill confirmed receipt with the actual content when asked.
  2. **War Room** — 13th roster entry in AiTeam (`components/WarRoom.jsx`): owner drops a topic → `run_war_room_meeting` (Haiku picks 2-4 relevant participants, Titan chairs open/turns/close), each turn posted live to `war_room` collection (frontend polls every 4s while meeting live). Closing transcript is parsed (EXTRACT_SYSTEM) into decision + action items → `team_tasks` + decision memo to participants + assignment memos to owners of advisory items. Routes: GET/POST/DELETE `/api/agents/war-room` (409 if meeting in progress). VERIFIED live: topic "build 2 flagships" → titan/forge/nova/quill debated, decision + 4 action items on task board UI.
  3. **Real task execution** — action items with `executable: forge_build|ivy_blog` are queued in `team_tasks` and run sequentially by `process_team_tasks` (forge_build waits for a free Forge slot, multi-count themes expand into distinct niches via NICHE_EXPAND). On completion: Market listing created, ✅ posted to War Room, completion memo Forge→Titan. Automation `_tick` resets stale running tasks/meetings and resumes queued tasks after restarts. VERIFIED live: "Harvest & Hearth" built + listed at $265, War Room post + memo confirmed.
  DB: `team_memos`, `team_tasks`, `war_room`, `war_room_meetings` (+indexes in server.py). Dev note: backend hot-reload kills in-flight asyncio builds; _tick recovery marks them error after 20 min.

- 2026-07-04 (later): **Automation wave 2 (4 features, all self-tested live)**:
  1. **Rex weekly auto-hunt** — `run_rex_weekly_hunt` in automation.py (weekly claim like forge_weekly): Haiku picks a fresh US city + no-website-prone category (avoiding recently hunted combos), runs the real Places hunt via shared `rex_hunt_and_report` (team.py), posts a lead-by-lead report to Rex's chat. Verified: Lubbock, TX landscaping (honest zero-lead report).
  2. **Mara Monday digest** — `run_mara_digest` posts a weekly analytics digest email draft (users/revenue/views/market/leads + 1 recommendation) to Mara's chat, Mondays ≥12:00 UTC. USER CHOSE chat-only (no Resend key yet); Resend playbook fetched, email hook can be added later. Verified live with real numbers.
  3. **"Upgrade to Premium" on TemplateView** — `POST /api/templates/{id}/upgrade` (409 if already premium, free for purchased/unlimited, else usage-metered) → mode="upgrade" in `_run_generation`. IMPORTANT: full-HTML premium polish TIMED OUT on 65KB sites; fixed by porting the flagship **addendum-injection pattern** into services/llm.py (`_premium_upgrade`: 2 tasks — behaviors + sections — returning STYLE/SECTIONS/SCRIPT blocks injected into head/footer/body, Sonnet w/ Haiku fallback per task). Sets quality="premium". UI: amber Gem "Upgrade to Premium" button ↔ "PREMIUM" badge (`upgrade-premium-btn`/`premium-badge`), confirm dialog, busy overlay label. Verified: Bean Haven 63KB→119KB premium.
  4. **War Room → Rex hunts** — new executable `rex_hunt` (brief "City, ST | category") in EXTRACT_SYSTEM/_create_tasks/process_team_tasks; `_exec_rex` parses brief (Haiku fallback parse), runs hunt, marks task done, posts ✅ to War Room + full report in Rex's chat. Verified E2E: meeting "have Rex hunt taco trucks in El Paso" → 7 real leads on the board.
  Dev note (recurring): hot-reload during in-flight asyncio jobs orphans them; automation _tick recovers (stale→error, queued resume).

- 2026-07-04 (security): **Security audit (security_audit_agent) → all findings fixed & verified**:
  - SEC-001 HIGH: published/preview HTML (`/api/p/{slug}` templates.py, `/api/market/{id}/preview` market.py) now served with CSP `sandbox allow-scripts allow-forms allow-popups allow-modals; connect-src 'none'` → opaque origin, no cookie/same-origin access. Verified: header present, sites render fully (DomainSite already used sandboxed iframe).
  - SEC-002: `/api/webhook/stripe-native` (subscriptions.py) fails CLOSED if STRIPE_WEBHOOK_SECRET missing; forged unsigned event → 400 (verified).
  - SEC-003: `_start_job` (llm.py) caps non-owners at 2 pending gen jobs (10-min window) before rate limit check.
  - SSRF: leads.py `_blocked_host` now resolves hostnames (getaddrinfo, run in thread) and blocks private/loopback/link-local/reserved IPs; `_fetch` follows redirects manually (max 4) re-validating each hop. Verified: 169.254.169.254 and DNS-rebind hosts (localtest.me) blocked.
  - Open redirect: `validate_origin` in security.py (CORS_ORIGINS allowlist + emergent domain regex) applied to checkout origin_url in payments.py/subscriptions.py/market.py. Verified: evil origin → 400, legit → session created.
  - Gotcha hit: inserting validate_origin mid-file split `check_rate_limit` body (SyntaxError) — fixed; verify insertion points against full function bodies.

- 2026-07-04 (later): **Rex Autopilot (5 features, all self-tested live)**:
  1. **3x/week auto-hunts** — rex hunt cadence changed from 7 days to 56 hours (automation.py `hunt_cutoff`).
  2. **Auto-demo for HOT leads** — `auto_demo_hot_leads` (automation.py): after any hunt (auto/manual/War Room), queues Forge demos for top HOT leads, capped at 2/week (`demo_auto` + `demo_queued_at` count), sequential builds, Rex announces each in chat. Verified: Marfa Burritos + Hot Dogs Kings demos auto-built to ready.
  3. **Pitch drafts** — `set_lead_outreach` (leads.py, OUTREACH_SYSTEM, haiku): personalized SMS (≤280ch, no lead's own phone in it) + call script w/ objection handler, stored on lead.outreach; demo URL (PUBLIC_BASE = first CORS origin = sitegenie.dev) becomes the hook and is auto-refreshed after demo completes. `auto_outreach_leads` drafts 5/hunt; on-demand via `POST /api/leads/{id}/outreach`. UI: "Pitch" button per lead row (`lead-pitch-{id}`) + modal (`pitch-modal`) with copy SMS/call buttons, demo link, Redraft, TCPA note — deliberately manual-send only (compliance).
  4. **Demo-view alerts** — `rex_demo_watch` runs every 15-min tick: compares template views_total vs lead.demo_views_seen; FIRST view silently absorbed (assumed owner's own check), subsequent views → "🔥 X just viewed their demo — strike NOW" with phone. Verified with simulated views.
  5. **Pipeline hygiene** — daily `run_rex_nudge` (14:00 UTC, job rex_pipeline_nudge): lists HOT leads untouched 5+ days with phones; archives lost leads >30 days (archived:true, filtered from GET /api/leads). Verified.

- 2026-07-04 (branding): **Official SiteGenie logo implemented** from user's uploaded asset. Processed via PIL (black bg → transparent): `/frontend/public/sitegenie_logo_mark.png` (genie emblem) + `sitegenie_logo_full.png` (full lockup w/ tagline). Emblem replaces all Zap-box marks: DashboardLayout sidebar+mobile, Landing nav/chat-avatars/footer, Market nav; favicon + tab title ("SiteGenie — Your Website Wish, Granted") in index.html. Full lockup used on Landing hero (h-40/48 + online badge below, `hero-logo-lockup`) and Login/Register headers (h-32). Unused Zap imports removed. Also: chat input right-padding (lg:pr-40) on AiTeam + WarRoom inputs so the "Made with Emergent" badge doesn't cover typing.
- 2026-07-04 (deploy fix): startup crash on Atlas "no primary" fixed — server.py startup now runs `_initialize()` as background task w/ 12×10s retries, never blocks/crashes; added DB-independent GET /health; database.py serverSelectionTimeoutMS=8000; quoted GOOGLE_PLACES_API_KEY in .env. deployment_agent PASS.

- 2026-07-04 (resilience): **"Waking the genie" outage banner**. New `GET /api/status` (server.py, DB ping w/ 3s timeout → {api, db}). Frontend: axios interceptor (lib/api.js) on network-error/5xx calls `reportApiError` (lib/apiHealth.js) which VERIFIES via /api/status before flagging (no false alarms from one-off 5xx like outreach 502); `GenieWaking.jsx` (mounted in App.js) shows fixed top banner "We're waking the genie — back in a moment." with genie mark + dots, polls /api/status every 8s, auto-clears on recovery. Live-tested: stopped mongodb → banner appeared on failed login; restarted → banner cleared. NOTE: production sign-in outage (Atlas unreachable in prod) is an INFRA issue — user directed to Emergent Support; this banner ships on next redeploy.

- 2026-07-04 (code review): **Functional code review (code_review_agent) → all findings fixed, testing agent verified 13/13 (iteration_16.json)**:
  - HIGH: legacy POST /checkout/session no longer accepts kind='subscription' (400; native /subscription/checkout only); apply_payment subscription branch removed; process_subscription (security.py) now LAPSES non-native active subs at period end (cancelled, plan_credits=0) instead of self-renewing forever with fake paid transactions.
  - Billing leak: _start_job (llm.py) enforces _min_cost per mode/quality (premium 8, quality/regenerate/upgrade 4, economy/edit 1) → 402; concurrency cap made race-safe via insert-then-count (429 + own job deleted).
  - Indexes added in _run_db_init (each try/except-wrapped): payment_transactions.session_id unique + user/created, gen_jobs, agent_jobs, agent_chats, war_room, automation_state, leads. NOTE: gen_jobs job_id unique creation skips with warning (legacy non-unique same-name index exists) — benign.
  - Unbounded arrays capped: agent_chats messages $slice -200 (post_agent_message + chat route), war_room $slice -300.
  - LOW: register duplicate-email race → 400; AiTeam pollForge interval now in ref + cleared on unmount.

- 2026-07-04 (data hygiene): **"Reset business data" owner tool (verified iteration_17.json, all pass)**. Root cause of "Titan reports untrue numbers on production": deploy seeded prod DB with a snapshot of preview test data. Fix: POST /api/auth/admin/reset-business-data (routes/auth.py, ResetBusinessInput {confirm:'RESET', include_leads}) — deletes non-owner users+sessions+their templates, all payment_transactions/subscriptions/gen_jobs/agent_jobs/agent_chats/war_room(+meetings)/team_memos/team_tasks/rate_events/login_attempts; KEEPS owner account+templates, market_listings, blog_posts, automation_state; leads optional. Owner session survives. UI: Billing page "Danger zone" (owner-role gated, `reset-business-btn`, prompt RESET + confirm leads). NOTE: preview data was wiped during testing (expected). USER MUST: redeploy, then run Reset on production /billing.

- 2026-07-05: **Launch Day milestone tracker** (self-tested: API + UI screenshot). GET /api/milestones (routes/plans.py, owner-only): computes 5 firsts live from DB excluding owner — first_signup (users), first_published (templates published non-owner), first_dollar (paid txn amount>0), first_subscriber (users subscription_status active), first_market_sale (txn kind market_purchase). Dashboard.jsx owner-only card `milestone-tracker` with achieved (emerald ✓ + detail + date) vs locked (dimmed hint) states, x/5 unlocked counter.

- 2026-07-06: **Team Pulse — make the AI Team feel ALIVE (user goal: agents continuously active, not only when spoken to)**. Shipped in one turn, testing agent 100% pass.
  1. **Activity log + status board** (`services/activity.py`): `log_activity(user_id, agent_id, kind, summary, detail, link)` → append-only `agent_activity` (capped 400) + refreshes a rolling `agent_status` board (idle/working + current_task + last_active). Instrumented every autonomous action to also log an activity: Titan briefing, Mara digest, Ivy article + Blaze social (blog.py), Forge weekly build + interactive Forge build (agents.py) + lead demo, Rex hunt/nudge/demo-view alert, War Room meetings + memos (team.py). `start_working`/`stop_working` mark live status.
  2. **Proactive pulse** (`maybe_proactive_pulse`, called each automation `_tick`): a THROTTLED loop (subtle cadence — min 20-min gap between any two proactive posts, per-agent 4h cooldown, rotates the stalest idle agent) where an idle agent drops one short (≤45-word) Haiku update grounded in the live `business_snapshot()`, posted to its chat (prefixed 💡) AND logged as kind=`pulse`. This is what makes the team visibly on-duty between the daily/weekly scheduled jobs.
  3. **API**: `GET /api/agents/activity?limit&before&agent_id` (feed + agents map) and `GET /api/agents/status` (statuses, working_now, latest, today_count) — both owner-gated.
  4. **Frontend** `components/TeamPulse.jsx`: (a) full **Team Pulse tab** — now the DEFAULT view in `AiTeam.jsx` (roster item `data-testid='pulse-item'` at top, live pulsing dot), with a "working now" strip (`working-now`), per-agent filter chips, and a live feed (`activity-feed`, rows `activity-<id>`) polling every 10s; rows deep-link to the agent's chat (`?agent=`) or open blog links. Roster agent avatars show a live pulsing green dot when that agent is working. (b) compact **Dashboard widget** (owner-only, `owner-team-pulse`) with "Team Pulse — live" + "Open the AI Team →". Deep-link via `useSearchParams` `?agent=`.
  DB: `agent_activity` [(user_id,created_at)], `agent_status` [(user_id,agent_id) unique] — indexes in server.py. Config knobs at top of activity.py (PULSE_TICK_SECONDS, PULSE_MIN_GAP_MINUTES, AGENT_PROACTIVE_COOLDOWN_HOURS). NOTE: `.env` files are gitignored — recreated backend/.env + frontend/.env in preview (owner neobeyondlegacy2@gmail.com / OwnerGenie2025!).

- 2026-07-06: **Halo goes public — customer support AI + feedback → Team Pulse + Halo-drafted website policy pages** (user goal: Halo at the forefront as the real customer support AI; route customer feedback into Team Pulse; have Halo write Refund Policy, FAQ, ToS as website pages). Testing agent 4/4 pass.
  1. **Public support chat** (`services/support.py` + `routes/support.py`): `POST /api/support/chat {message, session_id?, history?, contact_email?}` — NO AUTH, IP rate-limited (30/5min via `check_rate_limit`). Halo answers in a customer-facing voice grounded in `SITE_FACTS` (plans/prices from config, 15 free credits, credit packs, Template Market ownership+free edits+ZIP, publishing/domains/analytics). Never over-promises refunds — points to policy/FAQ. Conversations persisted in `support_chats` (per anon session_id, capped 100 msgs).
  2. **Feedback → Team Pulse**: after each reply a background `detect_customer_feedback` (Haiku) classifies the customer msg as feature/bug/complaint/praise/feedback; if actionable it stores to `customer_feedback` AND calls `log_activity(owner_id, 'halo', 'feedback', …)` so it appears in the owner's Team Pulse feed as a 'Customer' badge row (deep-links to /team?agent=halo). Verified E2E (mobile-app feature request surfaced in owner feed).
  3. **Halo-drafted website pages**: `PAGE_SPECS` for faq / refund / terms → Halo (Haiku) writes clean semantic HTML BODY grounded in SITE_FACTS, stored in `site_pages` (kind unique). `ensure_pages()` runs at startup to seed missing ones; `get_page` auto-seeds on first access. `GET /api/support/pages/{kind}` (public). Frontend `pages/PolicyPage.jsx` renders them at public routes `/faq`, `/refund-policy`, `/terms` with `.policy-content` styles (index.css); footer links added to Landing (`footer-faq/refund/terms`).
  4. **Frontend widget** `components/HaloWidget.jsx`: floating bottom-right bubble (`halo-launcher`, at bottom-20 to clear the Emergent badge) → premium chat panel (`halo-panel`, `halo-input`, `halo-send`, `halo-messages`) with greeting, quick prompts, anon session in localStorage (`sg_support_session`), unread dot. Mounted globally in App.js via `SupportWidget` which HIDES it for owner/admin and on `/team`. Added `feedback` kind (sky, "Customer" label) to TeamPulse KIND_META.
  DB: `site_pages` (kind unique), `support_chats` (session_id unique), `customer_feedback` [(created_at,-1)] — indexes in server.py. NOTE: pages are AI-drafted defaults — owner should review/adjust legal wording before relying on them.
  5. **Customer Feedback Inbox (owner)** — added 2026-07-06: owner-gated `GET /api/support/feedback?status&kind` (returns items + `by_status`/`by_kind` counts + `new_count`), `PATCH /api/support/feedback/{id} {status}` (new|reviewed|actioned|dismissed), `DELETE /api/support/feedback/{id}` (routes/support.py, FeedbackStatusInput). Frontend `components/FeedbackInbox.jsx` — opened from Halo's chat header toggle (`halo-inbox-toggle`), panel `feedback-inbox` with status tabs (`fb-status-*`) + type filters (feature/bug/complaint/praise/feedback with counts), per-item cards (`feedback-item-<id>`) showing kind badge + sentiment dot + verbatim quote + contact, and actions (`fb-review/action/dismiss/delete-<id>`, reopen). Clicking a 'Customer' row in Team Pulse deep-links `/team?agent=halo&inbox=1` which auto-opens the inbox (AiTeam effect on `?inbox=1`; TeamPulse passes `act.link` to `onOpenAgent(id, link)`; backfilled the 2 legacy feedback activity links). Testing agent 5/6 (deep-link fixed via backfill after test).

- 2026-07-07: **Luxury high-converting generation engine + exclusive ownership transfer** (user goal: SiteGenie builds high-converting, luxury-but-approachable full websites with strong hero/headline/subheadline/CTA + About with professional badges & cards; and clean/easy transfer of ownership when a site is sold; buyer can edit ALL aspects; CTAs open to real pages/sections). Testing agent all pass (backend E2E + 5/5 onboarding UI).
  1. **Upgraded prompts** (`services/llm.py`): rewrote `GEN_STRATEGY_SYSTEM` (hero headline+subheadline+primary&secondary CTA+trust strip, ABOUT with brand story + 3-4 value cards + 4 tailored TRUST BADGES, services cards, social proof + stats, FAQ, primary conversion action, luxury-but-approachable design direction) and `GEN_BUILD_SYSTEM` (required sections each with an id: home/about/services/faq/contact; FUNCTIONAL CTAs mandate — in-page CTAs smooth-scroll to section anchors, Call→tel:, Email→mailto:, contact form validates+success; luxury aesthetic w/ elegant display font). Also upgraded `_build_economy_prompt` + `_build_site_prompt`. Verified: Economy Haiku build produced 49KB site with all section ids, 20 in-page anchors, tel:+mailto:, trust badges + value cards.
  2. **Exclusive sale + ownership transfer** (`services/market.py::fulfill_market_purchase`): buyer copy gets `owned_exclusive=True, onboarded=False`; listing DELISTED (`active=False, sold=True, sold_to, sold_at, sold_template_id`) so one-of-one; `ownership_certificates` doc created (cert_id, title, price, buyer/seller, transferred_at) + linked on txn. Checkout already blocks re-purchase via `active:True`.
  3. **New template endpoints** (routes/templates.py): `PUT /api/templates/{id}/details` (TemplateDetailsInput; safe literal HTML find-replace so name/email/phone/color reflect in the site), `POST /api/templates/{id}/onboarded`, `GET /api/templates/{id}/certificate`.
  4. **Frontend**: `components/OwnershipOnboarding.jsx` — 3-step "Make it yours" modal (`onboarding-modal`: details→publish→done w/ ZIP + start editing) + `OwnershipCertificate` (`certificate-modal`). `MarketSuccess.jsx` success CTA → `/templates/<id>?onboard=1`. `TemplateView.jsx`: purchased sites show `make-it-yours-btn` + `ownership-cert-btn`; `?onboard=1` auto-opens for un-onboarded purchased sites.
  DB: `ownership_certificates` (cert_id unique, template_id index). NOTE: exclusive model — a listing now sells ONCE then leaves the Market (changed from re-sellable).

- 2026-07-07 (later): **P1 legal + PDF cert; P2 payment recovery** (testing agent 4/4 P1 UI pass; P2 verified at data layer).
  - **P1a Clickwrap consent** — Register.jsx required checkbox `register-consent` (submit `register-submit` disabled until checked; also gates Google signup) linking /terms /privacy /refund-policy. Backend `RegisterInput.consent`; register + google-session (new users) require it and store `legal_consent` on the user + an immutable `consent_events` doc (LEGAL_TERMS_VERSION="2026-07-07", ip, user_agent, agreed_at). Added **Privacy Policy** page (support.py PAGE_SPECS `privacy`) at /privacy (+ footer link, PolicyPage META).
  - **P1b PDF certificate** — added `reportlab==4.2.5`; `services/certificate_pdf.py::build_certificate_pdf` (branded gold-bordered cert). `GET /api/templates/{id}/certificate.pdf` (buyer-only). "Download PDF certificate" button (`download-cert-pdf`) in OwnershipCertificate modal.
  - **P2 Failed-payment recovery** — `services/recovery.py::run_payment_recovery` (called each automation tick): finds `payment_transactions` unpaid+unprocessed, older than 30min, within 72h, not yet `recovery_flagged`; claims each once; **Rex** posts a Team Pulse alert (kind=`recovery`) + **Mara** drafts a personalized win-back email into her chat (kind=`recovery`). Added `recovery` kind to TeamPulse KIND_META (emerald). Verified E2E: abandoned Monthly-plan checkout → Rex alert + Mara 1KB draft.
  - **REGRESSION FIXED**: frontend/.env REACT_APP_BACKEND_URL had drifted to genie-deploy-1 host != active preview host -> App.js rendered DomainSite ("No site connected to this domain") for the whole app. Fixed: `isCustomDomain` now also excludes any `*.emergentagent.com`/`*.emergent.host` host; reset .env to the active preview URL. DB: `consent_events` index added in server.py.
## Backlog
- ~~**Awaiting user: Google Places API key** to unlock Rex's hunting~~ **RESOLVED 2026-07-04** — user provided key, added to `backend/.env` as `GOOGLE_PLACES_API_KEY`. First hunt (Marfa, TX food trucks) surfaced 5 real no-website leads (Marfa Burritos 1,080★, El Turista 51★, the Water Stop 819★, Angel's 296★, Jacko's 16★) + Odessa handyman (Don's Plumbing 37★). Endpoint status code changed 502→424 in `routes/leads.py` so Cloudflare doesn't rewrite the error page (was hiding "IP restriction" message on the key). `services/leads.py` now bubbles specific Google 403 reasons (IP restriction / API disabled) so future misconfigurations are self-diagnosing.
- (user action) Redeploy to production (https://sitegenie.dev) to ship the Template Market + AI Team.
- 2026-07-07 (later): **Persistent Agent Brain — 12 agents remember & continue across data resets** (user goal: after Reset Business Data, everyone remembers what we were working on). Testing agent 4/4 pass. Scope: durable facts/preferences/goals/context + open threads + rolling summary; created auto + manual; brain ALWAYS preserved on reset; editable Memory viewer.
  - `services/memory.py`: `agent_memory` collection, one doc per (user_id, agent_id) with `facts[]` (kind fact|preference|goal|context, source auto|manual, capped 40), `open_threads[]` (capped 15), `rolling_summary`. `extract_and_store_memory` (background Haiku after each chat) distills the exchange into new facts / open+done threads / summary via EXTRACT_SYSTEM, deduped by normalized text; `agent_memory_context()` renders it for prompt injection; `add_fact`/`delete_memory_item` for manual edits. Also captures explicit "remember…/note that…" as facts.
  - Injection: `services/agents.py::agent_reply(..., user_id=)` + `_system_prompt(memory_ctx)` prepend the agent's persistent memory with instructions to treat it as true and continue open threads. War Room turns inject per-agent memory (`team.py::_war_system(memory_ctx)` + `_agent_mem`). Automation Titan briefing & Mara digest pass `user_id=owner_id`. Chat route fires `extract_and_store_memory` as a background task alongside memo detection.
  - Reset safety: `agent_memory` is NOT in `reset_business_data`'s delete list (added a code comment warning never to add it) — brain survives; only visible `agent_chats` are wiped, and memory is re-injected so agents continue. Verified: after simulating a reset (agent_chats wiped), Nova's Oct-1 goal + preferences still injected.
  - API (owner-gated): `GET/POST /api/agents/{id}/memory`, `DELETE /api/agents/{id}/memory/{mem_id}` (models.AgentMemoryInput). Frontend `components/AgentMemory.jsx` — "Memory" toggle (`memory-toggle`) in every agent header opens panel (`agent-memory`) with rolling summary + facts (`memory-fact-<id>`, delete `memory-delete-<id>`, manual "you" tag) + open threads (`memory-thread-<id>`); add via `memory-add-toggle`/`memory-add-input`/`memory-add-save`. DB: `agent_memory` (user_id,agent_id) unique index in server.py.
- 2026-07-07 (later): **Team Shared Brain + War Room decision auto-capture** (extends the persistent Agent Brain). Testing agent 4/4 pass.
  - **Shared brain** (`services/memory.py`): stored as an `agent_memory` doc with sentinel `agent_id=TEAM_AGENT="__team__"` (facts capped 60). `team_brain_context()` renders it; `agent_memory_context()` now PREPENDS the shared brain to every individual agent's context, so all 12 agents see team-wide facts (brand standards, decisions, company context) on top of their own memory. Helpers: `get_team_brain`, `add_shared_fact`, `delete_shared_item`, `add_open_thread`.
  - **War Room auto-capture** (`services/team.py::run_war_room_meeting` -> `record_war_room_decision`): when a meeting concludes, the DECISION is written to the shared brain (kind=`decision`) and each action item becomes an OPEN THREAD on the owning agent's own memory — durable + reset-proof. Verified: decision in shared brain AND threads on Quill & Ivy.
  - **API** (owner-gated): `GET/POST /api/agents/team-memory`, `DELETE /api/agents/team-memory/{mem_id}` (placed before the dynamic `/agents/{agent_id}/memory` routes).
  - **Frontend**: `AgentMemory.jsx` gained a `shared` prop (switches to team-memory endpoints, "Team Shared Brain" title). New **Team Brain** roster item in AiTeam (`team-brain-item`, violet, desktop + mobile strip) renders `<AgentMemory shared />` as a main-area view (`data-testid='team-brain'`) alongside Team Pulse / War Room. Both shared and per-agent brains are preserved across Reset Business Data.
- 2026-07-07 (later): **Ivy SEO Autopilot** — expanded Ivy from daily-blogger to a full SEO/GEO engine (inspired by babylovegrowth.ai; user approved building all, SKIPPED backlink network as un-fakeable). Backend 9/9 + frontend 6/6 testing agent pass. `services/seo.py` + `/api/agents/ivy/seo/*` (owner-gated), `components/SeoCenter.jsx` opened from Ivy header (`ivy-seo-toggle` -> `seo-center`, deep-link `?seo=1`). Features:
  1. **Keyword engine + 30-day content calendar** (`build_content_calendar`/`get_calendar`, `seo_calendar` coll): LLM keyword research -> topics with target/secondary keywords, intent, format, pillar, AI-search prompt. Daily blog (`blog.py::write_blog_post`) now pulls the next planned topic (`next_planned_topic`) instead of random; automation auto-refills when <3 planned remain.
  2. **Article Quality Score /100** (`score_article`, BLG-style checklist) + **auto-improve** (`improve_article`) when score <80; score stored on post + shown in Ivy msg + Team Pulse.
  3. **GEO / AI-visibility audit** (`geo_audit`): estimates if ChatGPT/Perplexity/Gemini would cite SiteGenie (0-100 + cited + content gap). **Technical SEO audit** (`technical_audit`) fetches any URL (reuses leads.py _fetch/_blocked_host SSRF guard), extracts signals, returns prioritized fixes. Stored in `seo_audits`.
  4. **Internal link / pillar map** (`internal_link_map`): pillars + cluster interlink suggestions.
  5. **Reddit/community opportunity finder** (`reddit_opportunities`): high-intent threads + non-spammy reply drafts (no posting).
  6. **SEO Command Center** UI (6 tabs: Overview/Calendar/AI Visibility/Site Audit/Link Map/Community). All Ivy SEO actions log to Team Pulse (kind=`seo`). Ivy role renamed "SEO & GEO Autopilot" + new quick-actions. DB indexes: `seo_calendar`, `seo_audits` in server.py.
- P3: Weekly analytics email digest (Resend) — Mara can draft the copy.
- P3: Scheduled autonomous agent runs (e.g., Titan posts a daily briefing to an activity feed automatically).

## Key Endpoints
- `POST /api/templates/generate` → job; poll job for template
- 2026-07-07 (later): **Ivy weekly GEO sweep + one-click gap->calendar** (extends SEO Autopilot). Backend verified (gap->calendar E2E; sweep completes as background task, writes trend + posts to Ivy chat + Team Pulse) + frontend 5/5 pass.
- 2026-07-07 (later): **UI-UX-Pro-Max skill "installed" + Conversational Build Canvas** (user: generation felt restricting, wanted it fluid like chat; provided their Claude skill repo). LLM = Anthropic Claude via Emergent key (STRATEGY_MODEL=claude-haiku-4-5, BUILD_MODEL=claude-sonnet-4-6). Testing agent 6/6 pass.
  - **Skill install** (Phase A): cloned github.com/nextlevelbuilder/ui-ux-pro-max-skill, vendored its pure-Python design engine + CSV knowledge into `backend/uiux_skill/` (search.py, design_system.py, core.py + data/*.csv; patched core.DATA_DIR to `Path(__file__).parent/'data'`). `services/design_intel.py::design_directive(fields)` runs `generate_design_system(query)` server-side (~1s, lru_cached) -> tailored design system (pattern, exact palette as CSS vars, font pairing, effects, anti-patterns) + distilled PRO_UI_RULES. Injected into `_build_site_prompt` + `_build_economy_prompt` via `_design_block`. Verified quality lift (elegant serif + gold luxury palette + SVG icons, no emojis).
  - **Conversational Build Canvas** (Phase B): `services/build.py` + `routes/build.py` — `build_sessions` collection; POST /api/build/session, POST /api/build/{id}/message (first msg = LLM intent->full build; later msgs = LLM classify tweak/major/chat -> live edit on same template), POST /api/build/{id}/attach/{tid}. Small "tweak" edits billed at half (min 1) in `_run_generation`. Added `stage` field to gen_jobs (designing/building/polishing/refining) surfaced in /api/templates/job/{id}. Frontend `pages/BuildCanvas.jsx` at `/generate` (old form -> `/generate/classic`): chat left + live iframe right, live stage status, typing indicator, starters, model/quality settings (default **Haiku + Economy** for fluid ~90-110s builds), desktop/mobile preview, "Open & publish" -> TemplateView.
  - **Bug fixed mid-test**: `services/seo.py` used `post_agent_message` without importing it (NameError in weekly GEO sweep) -> added `from services.agents import post_agent_message`.
  - DB: `build_sessions` (session_id unique) index. NOTE: Sonnet/Quality builds are ~2-4min (higher fidelity, slower); canvas defaults to fast tier, quality/premium opt-in via settings.
  - **Weekly GEO sweep** (`services/seo.py::run_weekly_geo_sweep`): audits the owner's top 5 buyer questions (`top_buyer_questions`, persisted in `seo_state` key=buyer_questions), computes average AI-visibility + cited count, stores week-over-week trend + 12-pt history (`seo_state` key=geo_trend), posts a trend update to Ivy's chat and Team Pulse (kind=`seo`). Runs weekly on the automation tick (`ivy_geo_sweep` job, 7-day claim) + manual trigger `POST /api/agents/ivy/seo/weekly-sweep`. `geo_trend()` read model; `geo_audit` now returns `audit_id` + `gap_used`.
  - **Gap -> calendar** (`gap_to_calendar`): `POST /api/agents/ivy/seo/audit/{audit_id}/to-calendar` turns a GEO audit's content_gap into a scheduled `seo_calendar` topic (LLM expands to title/keywords/intent/format; scheduled after the last item; marks audit `gap_used=true`; blocks double-add).
  - **Frontend** `SeoCenter.jsx`: Overview shows a weekly AI-visibility **trend sparkline** (`seo-geo-trend`) + "Run sweep now" (`seo-run-sweep`); GEO result has a **"Turn into a planned article"** button (`seo-add-gap`) -> success toast + "Added to calendar". DB: `seo_state` (user_id,key) unique index in server.py.
- `POST /api/templates/{id}/publish`, `GET /api/p/{slug}`, `GET /api/og/{slug}.png`, `GET /api/templates/{id}/export`
- 2026-07-07 (later): **Visual click-to-edit builder (Shopify-editor style)** — user wanted Shopify/Site-Journey-style section editing with dropdown add options, available on owned + PURCHASED sites so buyers edit without regenerating. Testing agent 5/5 pass. Approach B (overlay editor on existing self-contained AI HTML — no schema rebuild).
  - **Backend**: `PUT /api/templates/{id}/html` (HtmlSaveInput) saves raw edited HTML scoped to `{template_id, user_id}` (covers owner AND buyers since a purchased copy is under their user_id); strips leftover editor chrome/attrs server-side.
  - **Editor runtime** `frontend/src/lib/editorRuntime.js` (`EDITOR_RUNTIME` injected into the iframe before `</body>`): makes leaf text contenteditable; marks top-level `<section>` (fallback header/footer/divs) with hover toolbars (move up/down, duplicate, delete, background color input) + a per-section "+ Add" button; postMessage bus (ready/dirty/open-add/add-block/request-html/html); 8 insertable blocks (subheadline, text, button, image, list, quicklinks, divider, spacer); `cleanHtml()` serializes stripping all editor chrome/attrs.
  - **UI** `frontend/src/components/VisualEditor.jsx`: full-screen editor (`visual-editor`) with `editor-iframe` (sandbox allow-scripts allow-same-origin), desktop/mobile toggle, dirty-tracking Save (`editor-save` -> PUT html), close guard; add-block dropdown (`add-block-menu`, items `add-block-<kind>`). Opened from TemplateView "Edit" button (`visual-edit-btn`) next to "Edit with AI"; onSaved updates the preview. Existing Publish pushes edits live.

## Test Credentials
See `/app/memory/test_credentials.md` (Owner: neobeyondlegacy2@gmail.com)

- 2026-06-16: **Live Stripe key fix** — Replaced expired `STRIPE_SECRET_KEY` in backend/.env with user's validated live key (ends hJa48rI8); live `STRIPE_WEBHOOK_SECRET` (whsec_n2ow...) confirmed in place. Verified via curl in preview: all 3 subscription plans (monthly/quarterly/annual) create `cs_live_` checkout sessions. User must Redeploy to push to production.

- 2026-07-07 (later): **Pricing rebuilt to match Emergent exactly (plans + top-ups)**. Backend verified (plans + top-up checkout + invalid-plan 400; subscription checkout code correct, returns 503 only when STRIPE_SECRET_KEY absent in preview — works in prod). Frontend renders full Emergent-style page.
  - **config.py** `SUBSCRIPTION_PLANS`: free ($0, 15cr, not purchasable), standard ($20/mo, $17/mo annual, 50cr), pro ($200/$167, 120cr, MOST POPULAR), team ($300/$250, 750cr shared, 5 members). Each has amount/annual_amount/monthly_credits/team_members/purchasable/annual_available/tagline. Signup still grants 15 credits once. Build costs unchanged. No plan `unlimited` (owner unlimited stays via is_owner).
  - `CREDIT_PACKS` = Emergent's 6 top-ups: pack_5 (5/$1 intro), pack_100 (100/$20), pack_250 (250/$50), pack_500 (500/$100 Most popular), pack_3000 (3000/$500 20% bonus), pack_6000 (6000/$1000 20% off). One-time via emergent-proxied checkout; never expire.
  - **Dynamic Stripe subscriptions** (subscriptions.py): `/subscription/checkout {plan_id, billing, origin_url}` builds inline `price_data` (monthly=amount; annual=annual_amount*12 billed yearly) — NO pre-created Stripe Price IDs. Plan validation before Stripe-key gate so bad/free plans -> 400. SubCheckoutInput gained `billing`; txn stores billing.
  - **Frontend** `pages/Pricing.jsx` full rebuild: monthly/annual toggle (billing-toggle/monthly/annual, Save ~15%), 4 plan cards (plan-<id>, subscribe-<id>, MOST POPULAR on pro, EVERYTHING IN X PLUS bullets, SiteGenie labels), comparison-table, 6-tier topup-section (pack-<id>, buy-<id>) with badges + per-credit price.
  - NOTE: recurring env-drift — frontend/.env REACT_APP_BACKEND_URL flipped to genie-deploy-1 host (CORS-blocked /api/plans); reset to 5727d3a6 preview host. Production must set STRIPE_SECRET_KEY (+webhook) for live subscriptions.

- 2026-07-07 (later): **In-app Top-Up modal + Team multi-seat (shared credits)**. Top-up + team endpoints 11/11 test pass; team invite/accept/shared-pool/disband verified at data layer (live Stripe unavailable in preview).
  - **Top-Up modal** `components/TopUpModal.jsx`: click sidebar credit balance ("Top up" `sidebar-buy-credits`, or `mobile-credits`) -> modal (`topup-modal`) with the 6 Emergent tiers (`topup-buy-<id>`), reuses `/checkout/session` credits flow. Only for metered (non-unlimited) users. Wired into DashboardLayout.
  - **Team multi-seat** (`services/teams.py` + `routes/teams.py`, `/api/team-account*`): Team-plan owner gets a `teams` doc (owner + up to 4 = MAX_SEATS 5). Email invites (`team_invites`); invitee accepts (must match email). Members carry team_id/team_owner_id/team_role='member' and SHARE the owner's credit pool: `security.billing_user()` resolves a member's spend to the team owner; `_start_job`/`_run_generation` bill the resolved owner (added `bill_user_id`); `public_user_view()` overlays the shared pool for members (in /auth/me + login). Owner-only guards (403). Team created on Team-plan activation (billing `_activate_native_sub` -> ensure_team_for_owner; other plan -> disband_team); disbanded on cancel (webhook), lapse (process_subscription), downgrade -> members revert to Free. reset_business_data now clears teams/invites/build_sessions.
  - **Frontend** `components/TeamPanel.jsx` on Billing (`team-panel`): seats X/5 (`team-seats`), owner row, members (`team-member-<id>`, `remove-member-<id>`), pending invites (`revoke-invite-<id>`), invite form (`team-invite-email`/`team-invite-btn`), Leave; accept-invite card (`team-invites`/`accept-invite-<id>`) for pending invitees. Verified seats 1/5, invites render, 5-seat cap enforced.
  - DB: `teams` (team_id + owner_user_id unique), `team_invites` (invite_id unique, email+status). NOTE: store-'Owner' (is_owner) is distinct from Team 'owner' (billing user).
